import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    root:{
        width: '100%',
        position: 'relative',
        zIndex: 999,
    },

    img: {
        width: '100%',
        resizeMode: "cover",
        position: 'absolute',
    }
})